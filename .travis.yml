language: python

python:
  - "3.5"

install:
  - pip install -r requirements.txt
  - pip install .[test]

script: pytest --cov twyla.kubedeploy --cov-report term-missing
after_script:
  - pip install codecov
  - codecov
